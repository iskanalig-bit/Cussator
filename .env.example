# Names only, no values — set the real values in the Vercel dashboard
# (Project Settings > Environment Variables) or via `vercel env add`.
# For local dev without Vercel, these instead live in site/.env (gitignored).

ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
